import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    color: '#FFFFFF',
    marginBottom: 40,
    fontSize: 20,
    fontWeight: '600',
  },
  subTitle: {
    color: '#FFFFFF',
    marginBottom: 30,
    fontSize: 16,
  },
  timer: {
    color: '#FFFFFF',
  },
  playContainer: {
    paddingTop: 30,
  },
  timerContainer: {
    paddingTop: 20,
  },
  playImage: {
    height: 50,
    width: 50,
  },
  timesSelectContainer: {
    paddingTop: 30,
    flexDirection: 'row',
  },
});

export default styles;
